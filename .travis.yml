language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - hhvm
  - nightly

matrix:
  allow_failures:
    - php: hhvm
    - php: nightly
    - php: 5.5

before_install:
 - php composer.phar self-update
 - php composer.phar install

script:
 - phpunit --coverage-text

notifications:
  email:
    - andreas@heigl.org
